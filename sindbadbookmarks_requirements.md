# sindbadbookmarks 要件定義書

**プロジェクト名:** sindbadbookmarks（復活版）
**文書バージョン:** 1.0
**作成日:** 2026-04-01
**ステータス:** ドラフト

---

## 1. プロジェクト概要

LGBTコミュニティの伝説的ポータルサイト「sindbadbookmarks」を現代の技術スタックで復活させる。自薦・他薦を問わず、店舗・団体・メディアなどの情報をジャンル別・地域別・サービスカテゴリ別に登録・閲覧できるポータルサイトを構築する。

### 1.1 ビジョン

LGBTに関わるあらゆるリソースを「見つけられる・登録できる・一覧できる」場所を再び作り、コミュニティの可視性とアクセシビリティを高める。

### 1.2 技術スタック

| レイヤー | 技術 |
|---|---|
| データベース・認証・API | Supabase（PostgreSQL + Auth + Edge Functions） |
| Webサーバー・CDN・ホスティング | Cloudflare（Pages / Workers） |
| フロントエンド | 未定（Next.js / Nuxt / Astro 等、別途選定） |

---

## 2. ユーザーと役割

### 2.1 ロール定義

| ロール | 認証 | 権限 |
|---|---|---|
| **閲覧者（Visitor）** | 不要 | 情報の検索・閲覧・フィルタリング。ダッシュボードの件数確認 |
| **登録者（Contributor）** | 必須 | 閲覧者の権限＋情報の新規登録・自分が登録した情報の編集 |
| **管理者（Admin）** | 必須 | 全権限。登録情報の事後チェック・編集・非公開化・ユーザー管理 |

### 2.2 認証方式

- **Googleログイン**（Supabase Auth OAuth provider）
- **メールアドレス＋パスワード**（Supabase Auth email provider）
- 認証後、初回ログイン時にプロフィール（表示名のみ必須）を設定

---

## 3. 掲載対象の情報タイプ

| 情報タイプ | 説明 | 例 |
|---|---|---|
| **店舗** | 実店舗を持つ事業者 | バー、クラブ、カフェ、書店、サウナ等 |
| **団体・コミュニティ・NPO** | 組織・活動グループ | 支援団体、当事者グループ、NPO法人等 |
| **メディア・Webサービス・アプリ** | オンラインのリソース | Webメディア、マッチングアプリ、ポッドキャスト等 |

---

## 4. 分類体系（ジャンル・タクソノミー）

3つの独立した分類軸を持ち、各リスティングに複数タグを付与可能とする。

### 4.1 軸①：目的別カテゴリ

| カテゴリ | 説明 |
|---|---|
| 交流・出会い | ソーシャル、コミュニティイベント |
| 支援・相談 | カウンセリング、法律相談、医療 |
| ナイトライフ | バー、クラブ、パーティー |
| 文化・アート | 映画、書籍、展示、パフォーマンス |
| 情報・メディア | ニュース、ポッドキャスト、ブログ |
| 暮らし・サービス | 宿泊、美容、不動産、旅行 |
| 権利・アドボカシー | 法的支援、政策提言、啓発活動 |

### 4.2 軸②：業態別カテゴリ

| カテゴリ |
|---|
| 飲食 |
| 宿泊 |
| 美容・ファッション |
| 医療・メンタルヘルス |
| 法律・士業 |
| IT・テクノロジー |
| エンターテインメント |
| 教育・研究 |
| その他 |

### 4.3 軸③：フレンドリー度

| レベル | 定義 |
|---|---|
| **専門（Dedicated）** | LGBTを主な対象としたサービス |
| **フレンドリー（Friendly）** | LGBT対応を明示的に掲げている |
| **アライ（Ally）** | 理解・協力を表明している |

---

## 5. データモデル概要

### 5.1 主要テーブル

```
┌─────────────────────────────────────────────────────┐
│  profiles                                           │
│  ─────────                                          │
│  id (UUID, FK → auth.users)                         │
│  display_name (TEXT, NOT NULL)                       │
│  avatar_url (TEXT)                                   │
│  role (ENUM: visitor / contributor / admin)          │
│  created_at / updated_at                            │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│  listings（掲載情報）                                 │
│  ────────                                           │
│  id (UUID, PK)                                      │
│  title (TEXT, NOT NULL)                              │
│  description (TEXT)                                  │
│  listing_type (ENUM: shop / org / media)             │
│  friendliness (ENUM: dedicated / friendly / ally)    │
│  website_url (TEXT)                                  │
│  phone (TEXT)                                        │
│  email (TEXT)                                        │
│  image_url (TEXT)                                    │
│  nomination_type (ENUM: self / other)                │
│  status (ENUM: published / hidden / flagged)         │
│  submitted_by (UUID, FK → profiles)                  │
│  created_at / updated_at                            │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│  listing_locations（所在地）                           │
│  ──────────────────                                  │
│  id (UUID, PK)                                      │
│  listing_id (UUID, FK → listings)                    │
│  country (TEXT, NOT NULL)  例: "JP"                  │
│  region (TEXT)             例: "東京都"               │
│  city (TEXT)               例: "新宿区"               │
│  address (TEXT)                                      │
│  lat (DECIMAL)                                      │
│  lng (DECIMAL)                                      │
│  is_online_only (BOOLEAN, DEFAULT false)             │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│  categories（カテゴリマスタ）                          │
│  ──────────                                         │
│  id (UUID, PK)                                      │
│  axis (ENUM: purpose / industry / friendliness)      │
│  slug (TEXT, UNIQUE)                                 │
│  label_ja (TEXT)                                     │
│  label_en (TEXT)                                     │
│  sort_order (INT)                                   │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│  listing_categories（多対多リレーション）               │
│  ────────────────────                                │
│  listing_id (UUID, FK → listings)                    │
│  category_id (UUID, FK → categories)                 │
│  PK: (listing_id, category_id)                       │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│  regions（地域マスタ）                                │
│  ───────                                            │
│  id (UUID, PK)                                      │
│  country_code (TEXT)      例: "JP"                   │
│  region_name (TEXT)       例: "東京都"                │
│  display_order (INT)                                │
│  is_overseas (BOOLEAN)                              │
└─────────────────────────────────────────────────────┘
```

### 5.2 ER図（リレーション概要）

```
profiles ──1:N──► listings ──1:1──► listing_locations
                      │
                      ├──N:M──► categories（via listing_categories）
                      │
                      └──N:1──► regions（via listing_locations.region）
```

---

## 6. 機能要件

### 6.1 MVP Phase 1：ダッシュボード＋認証

**優先度：最高**

**ダッシュボード（トップページ）**
- ジャンル別（目的別・業態別）の登録件数をカード形式で表示
- 地域別の登録件数を日本地図 or リスト形式で表示
- フレンドリー度別の件数表示
- 情報タイプ別（店舗/団体/メディア）の件数サマリー
- 各件数カードからワンクリックでフィルタ済み一覧へ遷移

**認証・アカウント**
- Googleログイン（OAuth 2.0 via Supabase Auth）
- メール＋パスワードによるサインアップ／ログイン
- パスワードリセット（メール送信）
- 初回ログイン時のプロフィール設定（表示名）
- ログアウト

### 6.2 MVP Phase 2：情報登録・検索・一覧表示

**優先度：高**

**情報登録フォーム（要ログイン）**
- 基本情報入力：タイトル、説明文、情報タイプ、URL、連絡先
- 自薦／他薦の選択
- 分類タグ選択：目的別（複数可）、業態別（複数可）、フレンドリー度（単一）
- 所在地入力：国、都道府県/地域、市区町村、住所（任意）
- オンライン専用フラグ
- 画像アップロード（1枚、任意）
- 登録後は即時公開（status: published）

**検索・フィルタ**
- キーワード全文検索（タイトル＋説明文）
- 分類軸ごとのフィルタ（目的別・業態別・フレンドリー度）
- 情報タイプフィルタ（店舗/団体/メディア）
- 地域フィルタ（国 → 地域 → 市区町村の段階的絞り込み）
- 複合フィルタ（上記を組み合わせ可能）

**一覧表示**
- カード形式の一覧（画像サムネイル、タイトル、タグ、地域）
- 件数表示
- ページネーションまたは無限スクロール
- ソート：新着順／名前順

**詳細ページ**
- 登録情報の全項目表示
- 地図表示（緯度経度がある場合）
- 外部リンク（Webサイト）
- 登録者情報（表示名のみ）
- 編集ボタン（登録者本人のみ表示）

### 6.3 MVP Phase 3：管理者パネル

**優先度：中**

- 全リスティング一覧（ステータスフィルタ付き）
- 個別リスティングの編集・ステータス変更（published / hidden / flagged）
- フラグ付きリスティングの優先表示
- ユーザー一覧・ロール変更
- カテゴリマスタの追加・編集・並び替え
- 地域マスタの追加・編集

---

## 7. 承認・モデレーションフロー

```
登録者が情報を送信
       │
       ▼
  即時公開（status: published）
       │
       ▼
  管理者が事後チェック
       │
       ├─ 問題なし → そのまま
       ├─ 要修正 → status: flagged（登録者に通知）
       └─ 不適切 → status: hidden（非公開化）
```

---

## 8. 非機能要件

| 項目 | 要件 |
|---|---|
| パフォーマンス | ダッシュボード表示 2秒以内、一覧ページ 1秒以内 |
| 可用性 | Cloudflare + Supabase のマネージドインフラに依存（99.9%目標） |
| セキュリティ | Supabase RLS（Row Level Security）で全テーブルにポリシー適用 |
| レスポンシブ | モバイルファースト。SP / Tablet / PC 対応 |
| SEO | SSR or SSG でメタタグ・OGP対応。各詳細ページに固有URL |
| 多言語 | Phase 1 は日本語のみ。将来的に英語対応を想定し、label_ja / label_en 構造を先行導入 |
| アクセシビリティ | WCAG 2.1 AA 準拠を目標 |
| 画像ストレージ | Supabase Storage を利用 |

---

## 9. Supabase RLS ポリシー方針

| テーブル | SELECT | INSERT | UPDATE | DELETE |
|---|---|---|---|---|
| listings | 全員（published のみ） | contributor 以上 | 本人 or admin | admin のみ |
| listing_locations | 全員 | contributor 以上 | 本人 or admin | admin のみ |
| listing_categories | 全員 | contributor 以上 | 本人 or admin | admin のみ |
| profiles | 全員（display_name のみ） | 自動（auth trigger） | 本人 or admin | admin のみ |
| categories | 全員 | admin のみ | admin のみ | admin のみ |
| regions | 全員 | admin のみ | admin のみ | admin のみ |

---

## 10. 画面一覧

| # | 画面名 | パス（案） | 認証 |
|---|---|---|---|
| 1 | ダッシュボード（トップ） | `/` | 不要 |
| 2 | 一覧ページ | `/listings` | 不要 |
| 3 | 詳細ページ | `/listings/:id` | 不要 |
| 4 | 新規登録フォーム | `/listings/new` | 必須 |
| 5 | 編集フォーム | `/listings/:id/edit` | 必須（本人/admin） |
| 6 | ログイン | `/login` | — |
| 7 | サインアップ | `/signup` | — |
| 8 | プロフィール設定 | `/profile` | 必須 |
| 9 | 管理者パネル | `/admin` | 必須（admin） |
| 10 | 管理：リスティング管理 | `/admin/listings` | 必須（admin） |
| 11 | 管理：ユーザー管理 | `/admin/users` | 必須（admin） |
| 12 | 管理：マスタ管理 | `/admin/master` | 必須（admin） |

---

## 11. MVP ロードマップ

```
Phase 1（4-6週）  ダッシュボード＋認証
  ├─ Supabase プロジェクト構築・DB スキーマ作成
  ├─ 認証（Google OAuth + Email/Password）
  ├─ ダッシュボード UI（件数集計ビュー）
  └─ Cloudflare Pages デプロイ

Phase 2（4-6週）  情報登録・検索・一覧
  ├─ 登録フォーム（自薦/他薦）
  ├─ 検索・フィルタ・一覧表示
  ├─ 詳細ページ
  └─ 画像アップロード

Phase 3（3-4週）  管理者パネル
  ├─ 管理者ダッシュボード
  ├─ リスティング管理（事後チェック）
  ├─ ユーザー管理
  └─ マスタデータ管理

Phase 4（将来）  拡張機能
  ├─ 英語対応
  ├─ レビュー・口コミ機能
  ├─ イベント情報の追加
  ├─ お気に入り・ブックマーク
  └─ APIの外部公開
```

---

## 12. 未決事項・今後の検討

| # | 項目 | 状態 |
|---|---|---|
| 1 | フロントエンドフレームワークの選定 | 未定 |
| 2 | ドメイン名の確保 | 未定 |
| 3 | 初期データの投入方法（旧サイトからの移行等） | 要検討 |
| 4 | 通知機能の要否（flagged 時のメール通知等） | 要検討 |
| 5 | 画像の最大サイズ・リサイズポリシー | 要検討 |
| 6 | 利用規約・プライバシーポリシーの作成 | 要作成 |
| 7 | 不適切コンテンツの通報機能 | Phase 4 候補 |
| 8 | 海外地域マスタの粒度（国→都市レベル？） | 要検討 |
